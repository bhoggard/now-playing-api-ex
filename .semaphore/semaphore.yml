version: v1.0
name: Sempaphore CI
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: Tests
    task:
      prologue:
        commands:     
          - checkout
          - mix local.rebar --force
          - mix deps.get
      jobs:
        - name: Tests
          commands:
            - mix test
